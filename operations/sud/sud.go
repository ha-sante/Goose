package goose

// Save is used for saving a document
func Save(){
	
	// Takes an already made document from a model and saves to the database
	// ctx, _ = context.WithTimeout(context.Background(), 5*time.Second)
	// res, err := collection.InsertOne(ctx, bson.M{"name": "pi", "value": 3.14159})
	// id := res.InsertedID

	

}

// Update is used for updating a document
func Update(){

}

// Delete is used for deleting a document
func Delete(){
	
}